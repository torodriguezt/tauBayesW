% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mo.bqr.svy.R
\name{mo_prior_default}
\alias{mo_prior_default}
\title{Default prior for Multiple-Output BQR (EM)}
\usage{
mo_prior_default(
  p,
  beta_mean = rep(0, p),
  beta_cov = diag(1e+06, p),
  sigma_shape = 0.001,
  sigma_rate = 0.001,
  names = NULL
)
}
\arguments{
\item{p}{Number of regression coefficients (including the intercept).}

\item{beta_mean}{Numeric vector of length \code{p}. A scalar is expanded to length \code{p}.}

\item{beta_cov}{Prior covariance for coefficients. May be:
\itemize{
\item a \code{p x p} matrix,
\item a scalar (expanded to \code{diag(scalar, p)}), or
\item a length-\code{p} vector (expanded to \code{diag(vector)}).
}}

\item{sigma_shape}{Positive scalar (shape of the inverse-gamma prior on \eqn{\sigma^2}).}

\item{sigma_rate}{Positive scalar (rate of the inverse-gamma prior on \eqn{\sigma^2}).}

\item{names}{Optional coefficient names to attach to \code{beta_mean} and \code{beta_cov}.}
}
\value{
An object of class \code{"mo_bqr_prior"} with fields \code{beta_mean},
\code{beta_cov}, \code{sigma_shape}, and \code{sigma_rate}.
}
\description{
Creates a prior object (class \code{"mo_bqr_prior"}) for \code{\link{mo.bqr.svy}}.
The regression coefficients have a multivariate normal prior, and the noise
variance has an inverse-gamma prior specified via shape/rate.
}
\examples{
pr <- mo_prior_default(p = 3, beta_mean = 0, beta_cov = 1e6)
pr
}
