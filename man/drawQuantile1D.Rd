% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_quantile.R
\name{drawQuantile1D}
\alias{drawQuantile1D}
\title{Draw univariate quantile curves/bands for mo.bqr.svy (d = 1)}
\usage{
drawQuantile1D(
  fit,
  datafile = NULL,
  response = "Y",
  x_var = NULL,
  x_grid = NULL,
  xValue = NULL,
  paintedArea = TRUE,
  band_choice = c("minmax", "symmetric"),
  print_plot = TRUE,
  show_data = !is.null(datafile)
)
}
\arguments{
\item{fit}{mo.bqr.svy object with response_dim = 1}

\item{datafile}{optional data.frame to overlay observed points}

\item{response}{name of Y column in datafile (character, length 1)}

\item{x_var}{which predictor to map to the horizontal axis (character, length 1).
If NULL, uses the first term label in fit$terms.}

\item{x_grid}{numeric vector of x values for x_var; if NULL, deduced from datafile or set to seq(-2,2,len=100)}

\item{xValue}{data.frame or list with fixed values for other predictors (one or more rows).
If multiple rows are provided, curves are colored by row (comparison).}

\item{paintedArea}{if TRUE and there are at least 2 taus, fills ribbon between two taus (see band_choice)}

\item{band_choice}{character: "minmax" (default) or "symmetric".
- "minmax": banda entre min(taus) y max(taus)
- "symmetric": toma el mayor tau < 0.5 y el menor tau > 0.5 (si existen)}

\item{print_plot}{if TRUE returns ggplot; if FALSE returns a data.frame with predictions}

\item{show_data}{if TRUE and datafile/response/x_var available, shows observed points}
}
\value{
ggplot object or data.frame with columns: xid, x, tau, yhat
}
\description{
Draw univariate quantile curves/bands for mo.bqr.svy (d = 1)
}
